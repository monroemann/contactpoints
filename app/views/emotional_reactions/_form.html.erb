<%= form_with(model: emotional_reaction) do |form| %>
  <% if emotional_reaction.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(emotional_reaction.errors.count, "error") %> prohibited this emotional_reaction from being saved:</h2>

      <ul>
        <% emotional_reaction.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
