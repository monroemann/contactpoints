<p id="notive" style="color: green"><%= notice %></p>

<h1>Contact Types</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @contact_types.each do |contact_type| %>
      <tr>
        <td><%= contact_type.name %></td>
        <td><%= link_to 'Show this contact type', contact_type %></td>
        <td><%= link_to 'Edit', edit_contact_type_path(category) %></td>
        <td><%= link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Category', new_contact_type_path %>
