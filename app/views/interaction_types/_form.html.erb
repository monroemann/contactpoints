<%= form_with(model: interaction_type) do |form| %>
  <% if interaction_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(interaction_type.errors.count, "error") %> prohibited this interaction_type from being saved:</h2>

      <ul>
        <% interaction_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
