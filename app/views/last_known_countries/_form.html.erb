<%= form_with(model: last_known_country) do |form| %>
  <% if last_known_country.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(last_known_country.errors.count, "error") %> prohibited this last_known_country from being saved:</h2>

      <ul>
        <% last_known_country.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block; border-radius: 5px" %>
    <%= form.text_field :name, required: true %>
  </div>

  <br>
  
  <div>
    <%= form.submit %>
  </div>
<% end %>
